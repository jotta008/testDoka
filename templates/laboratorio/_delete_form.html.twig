<form method="post" action="{{ path('app_laboratorio_delete', {'id': laboratorio.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ laboratorio.id) }}">
    <button class="btn">Delete</button>
</form>
